# 调货助手 - 版本更新日志

## 未发布 · 架构收敛 (2026-09-09)

- 批次与机型共用删除保护，财务启用后有关联历史批次的机型不可删除。
- 月报统一实际账本口径，展示当月客户净收款，移除报价税务估算与回款率。
- JSON 与 SQLite 统一完整恢复：隔离预检、业务对账、安全备份、失败回滚；不支持跨库合并。
- 清理无调用者的主窗口转发，增加删除、月报、恢复及界面回归测试。
- 保持 schema v6 与应用版本不变；本轮没有发布新版本。

## v1.16 (2026-08-04)

- 数据库升级到 schema v4，新增资金账户、收支分类、不可变账本事件和平衡分录
- 新增财务启用向导，按启用日建立期初资金、库存、应收和应付，启用前阻止财务写入
- 入库、出库、客户收款和供应商付款在同一事务中自动记账，支持预收预付及 FIFO 自动抵扣
- 出库保存不可变销售额与成本快照；报价不确认收入，利润按实际出库日期统计
- 新增整单/部分销售退货与采购退货，覆盖重新入库、报损、现金退款和余额保留
- FinanceTab 重组为经营总览、日常收支、应收应付、利润分析和流水审计五个子页
- 新增手工收入、费用、账户转账、资金调整、账户核对及财务 Excel 导出
- JSON 升级为 schema v4，完整保存账户、分配、账本、冲销、更正和退货关系
- 自动对账增加账本平衡、账户余额、往来余额、预收预付、成本快照和退货守恒检查

## v1.15 (2026-08-04)

- 数据库升级到 schema v3，移除采购价、报价、已收、付款及余额的旧 REAL 字段
- 删除 `src.models.database` 兼容门面，运行代码统一使用 Service、Repository 和 Queries
- JSON v1.15 仅导出整数分，同时继续兼容导入 v1.04/v1.13/v1.14 元金额
- FinanceTab 增加账务审计子页，可追踪入库、报价、收付款、FIFO 分配、冲销和更正
- 迁移前备份包含 SHA-256，金额不一致时完整回滚并明确报告记录

## v1.14 (2026-08-04)

- 移除 UI 中的数据库连接和直接 SQL，统一通过 Query/Service 访问数据
- 主窗口及全部 UI 移除 `src.models.database` 兼容门面依赖
- OrderService、InventoryService、PaymentService 的 SQL 全部下沉至 Repository
- 新增 ProductService、CustomerService、SupplierService，主数据写操作纳入事务与审计
- 已报价、已出库或已收款记录会阻止所属批次/机型被删除，避免历史链路隐藏
- 新增库存、收款分配、供应商余额、金额双写、外键和 SQLite 完整性自动对账
- 历史库存差异作为只读指标展示，明确异常不再伪装为迁移失败
- 新增“数据安全”菜单，可手动运行对账并从 SQLite 备份恢复
- 支持立即创建带 SHA-256 的 SQLite 备份，并导出文本对账报告
- 恢复前自动生成 `pre_restore_v1.14` 安全备份，恢复源损坏或版本过高时拒绝覆盖
- 新增 v1.14 架构约束、删除保护与恢复测试，全量测试达到 203 项

## v1.13 (2026-08-01)

- 新增 OrderService、InventoryService、PaymentService 和事务边界
- 引入 schema v2、版本化迁移、WAL 安全备份与失败回滚
- 金额改用整数分作为业务真值，保留 REAL 字段兼容一个版本
- 客户收款支持 FIFO 报价分配，流水修改/删除改为更正与冲销
- 主数据和报价支持软删除，并新增结构化审计事件
- 财务页从 MainWindow 拆分为独立 FinanceTab
- JSON v1.13 支持 cents、软删除、分配与审计数据事务恢复
- 统一应用、窗口、备份和打包版本号

## v1.03 (2026-05-24)

### 新增功能

1. **自动备份系统**
   - 程序启动时自动备份数据库
   - 备份保留最近 7 个版本
   - 备份位置：`data/backup/` 文件夹
   - 状态栏显示备份状态

2. **操作日志功能**
   - 记录关键操作（新增/修改/删除）
   - 支持查询操作历史
   - 数据库表：`operation_logs`

3. **客户购买历史**
   - 客户管理页面新增「购买历史」面板
   - 显示客户的总成交、总金额、总毛利统计
   - 列出客户所有报价记录详情

4. **JSON 数据备份**
   - 全量导出所有数据为 JSON 格式
   - 支持导入 JSON 备份恢复数据
   - 自动处理外键关系映射
   - 导出位置：桌面

### 修复问题

1. **库存实时更新**
   - 修复机型列表库存不实时更新的问题
   - 报价/新增/删除批次后，机型库存统计自动刷新
   - 确保库存数据始终准确

### 数据库变更

1. 新增表：
   - `operation_logs` - 操作日志表

2. 数据完整性：
   - 报价记录现在关联客户购买历史
   - 支持多表联合查询

### 文件变更

- `src/models/database.py` - 添加备份、日志、购买历史函数
- `src/utils/json_export.py` - 新增 JSON 导入导出模块
- `src/main.py` - 客户历史面板、备份集成

### 测试验证

✅ 数据库功能测试 - 通过  
✅ 库存计算测试 - 通过  
✅ JSON 导入导出 - 通过  
✅ 客户统计功能 - 通过  

---

## v1.02 (2026-05-24)

### 新增功能

1. **报价数量支持**
   - 报价面板新增「报价数量」输入框，支持一次报价多台设备
   - 报价记录列表新增「数量」列显示
   - Excel 导出包含数量字段

2. **实时库存管理**
   - 报价时自动扣减所选批次的库存数量
   - 报价前检查库存，不允许超库存报价（库存不足时弹出提示）

### 修复问题

1. **客户列表消失问题**
   - 修复客户管理页面初始化时客户列表不显示的问题
   - 程序启动时自动刷新客户列表和报价记录

2. **备注列显示**
   - 修复机型管理列表中备注列不显示的问题

### 优化改进

1. **统计计算优化**
   - 报价记录统计（总购入、总报价、毛利）已考虑数量因素
   - 毛利 = (对外报价 - 购入价) × 数量

2. **用户体验改进**
   - 报价成功后自动刷新批次库存显示
   - 库存不足时显示详细的提示信息

### 数据库变更

1. `quotes` 表新增字段：
   - `quote_quantity` (INTEGER): 报价数量，默认值为 1

### 文件变更

- `src/models/database.py` - 添加扣减库存函数、数量字段
- `src/main.py` - 报价逻辑更新、客户列表初始化
- `src/utils/excel_export.py` - 导出包含数量字段

---

## v1.01 (2026-05-23)

### 新增功能

1. **备注列显示**
   - 机型管理列表新增「备注」列
   - 编辑机型时支持备注的读写

### 修复问题

1. **Word 解析修复**
   - 修复 word_parser.py 中内存和硬盘提取代码缺失问题

### 优化改进

1. **打包优化**
   - 更新打包配置，支持版本号显示

---

## v1.0 (2026-05-22)

### 初始版本

1. **机型管理** - 管理电脑配置信息
2. **库存批次** - 记录进货批次和库存
3. **客户管理** - 管理客户联系方式
4. **报价功能** - 生成报价文本和图片
5. **Word导入** - 从上游价格表批量导入机型
6. **Excel导出** - 导出报价记录
## v1.17 (2026-08-09)

- 新增不可变库存流水与历史库存完整回建。
- 支持一张报价人工分配多个同型号批次出库。
- 出库、销售退货、采购退货按原批次精确追踪数量、成本与 SN。
- 新增库存流水、出库分配对账及 JSON v5 备份恢复。

### v1.17 修复（2026-09-08）

- 修复退货再售后同一 SN 可重复出库的问题，按原出库分配计算 SN 占用。
- 拦截同次退货中的重复 SN，以及原出库分配中已退货的 SN。
- 新增 schema v6 销售退货分配表，不回库退货同样扣减批次可退数量。
- 支持旧版退货分配回建及 JSON 备份恢复；缺失且无法还原的历史跨批次退货会在对账中提示，并阻止继续退货。
- 隔离 Windows 打包时的 DLL 搜索路径，修复外部 Poppler ICU 库混入导致的 QtWidgets 启动报错。
